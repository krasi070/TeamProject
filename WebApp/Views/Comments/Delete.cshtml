@model WebApp.Models.Comment

@{
    ViewBag.Title = "Delete Comment";
}

<body class="striped-background">

    <h2>Delete</h2>

    <h3>Are you sure you want to delete this comment?</h3>
    <div>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                Post Title
            </dt>

            <dd>
                @Html.DisplayFor(model => model.ForumPost.Title)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Text)
            </dt>

            <dd>
                @Html.DisplayFor(model => model.Text)
            </dd>

            <dt>
                @Html.DisplayNameFor(model => model.Date)
            </dt>

            <dd>
                @string.Format("{0}.{1}.{2}",
                        Model.Date.Day.ToString().PadLeft(2, '0'),
                        Model.Date.Month.ToString().PadLeft(2, '0'),
                        Model.Date.Year)
            </dd>

        </dl>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="form-actions no-color delete-comment-button">
                <input type="submit" value="Delete" class="btn btn-default" />
            </div>
        }
    </div>
</body>